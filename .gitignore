# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets / local config
.env
secrets/
*.pem

# Claude Code local (machine-specific) settings
.claude/settings.local.json

# Runtime state (processed-transaction ids) — not business data.
# Anchored to repo root so it does NOT match src/expenses/state/.
/state/
